# Epic QA Agent — Homebrew tap

Install with:

```bash
brew install micfd/epic-qa-agent/epic-qa-agent
```

Updates roll out via Sparkle auto-update inside the app; `brew upgrade`
picks up new versions published here.

Source repo: https://github.com/micfd/epic-qa
